package inf112.skeleton.app.gameobjects;

public enum GameObjectType {
    CONVEYOR,
    FLAG,
    HOLE,
    LASER_OUTLET,
    PLAYER,
    PUSHER,
    REPAIR_STATION,
    STANDARD_TILE,
    WALL,
    ROTATOR_CLOCKWISE,
    ROTATOR_COUNTER_CLOCKWISE,
    LASER_BEAM;
}
