package inf112.skeleton.app.gameobjects;

public enum GameObjectType {
    CONVEYOR,
    FLAG,
    HOLE,
    HUNTER,
    LASER_BEAM,
    LASER_OUTLET,
    PLAYER,
    PUSHER,
    REPAIR_STATION,
    STANDARD_TILE,
    ROTATOR_COUNTER_CLOCKWISE,
    ROTATOR_CLOCKWISE,
    TELEPORTER,
    WALL
}
