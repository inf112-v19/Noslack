package inf112.skeleton.app.gameobjects;

public enum GameObjectType {
    CONVEYOR,
    FLAG,
    HOLE,
    LASER_BEAM,
    LASER_OUTLET,
    ROBOT,
    PUSHER,
    REPAIR_STATION,
    STANDARD_TILE,
    ROTATOR_COUNTER_CLOCKWISE,
    ROTATOR_CLOCKWISE,
    TELEPORTER,
    WALL
}
